# Implementation Plan — Codebase Audit 2026-05-14 12:25

## Overview

23 total findings (0 Critical, 1 High, 7 Medium, 15 Low). Estimated total effort: ~6-8 hours. Phases 1-2 cover the highest-impact items in ~2 hours.

## Phase 1: Security (Do First)

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 1 | H1: nginx security headers stripped from static assets | `nginx.conf:19-22` | S | Duplicate all four `add_header` directives (X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Content-Security-Policy) inside the static assets `location ~*` block. Rebuild and verify with `curl -I` against a JS file. |
| 2 | M1: applyRequirements pushes past max length | `src/App.jsx:32`, `src/wordListUtils.js:178-195` | S | In `App.jsx:generatePassword`, calculate `reqExtra = (requirements.number ? 1 : 0) + (requirements.special ? 1 : 0)` and pass `maxLength - reqExtra` to `getRandomWordsWithinLength`. Add a test for this edge case. |
| 3 | O1: Duplicate secureRandomInt | `src/wordListUtils.js:10-23`, `src/humorEngine.js:168-178` | S | Create `src/crypto.js` exporting `secureRandomInt(max)`. Import from both `wordListUtils.js` and `humorEngine.js`. Remove the duplicate implementations. |

## Phase 2: Testing & Documentation

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 4 | M3: Missing tests — humorEngine | `src/humorEngine.js` → new `src/humorEngine.test.js` | M | Test: (a) `generateFunnyPassphrase` returns words from its categories, (b) respects min/max length constraints, (c) uses crypto.getRandomValues not Math.random, (d) returns success:false for impossible constraints, (e) all template categories exist in the categories object. |
| 5 | M3: Missing tests — RequirementsToggles | `src/components/RequirementsToggles.jsx` → new `src/components/RequirementsToggles.test.jsx` | S | Test: (a) renders all three toggles, (b) calls onChange with the correct key when toggled, (c) reflects checked state from requirements prop. |
| 6 | M3: Missing tests — ErrorBoundary | `src/ErrorBoundary.jsx` → new `src/ErrorBoundary.test.jsx` | S | Test: (a) renders children when no error, (b) renders error UI when child throws, (c) refresh button exists. |
| 7 | M4: Update README.md | `README.md` | S | Replace MUI references with custom CSS/"Terminal" theme, update "three-word" to "2-6 word", change Node.js version to 22, add sections for humor engine, requirements toggles, and Vite. Remove old MUI feature bullets. |
| 8 | Q1: Update structure YAML | `.claude/structure/src.yaml` | S | Rename `index.js` → `main.jsx`, `App.js` → `App.jsx`, `ErrorBoundary.js` → `ErrorBoundary.jsx`. Update descriptions to remove MUI references. Add `humorEngine.js` and all 6 component files. Update `index.css` description to "Terminal design system CSS with custom properties, component styles, and animations". |

## Phase 3: UX & Assets

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 9 | U1: Missing favicon and logo | `public/favicon.ico`, `public/logo192.png` | S | Generate a simple lock/terminal icon as favicon.ico (multi-size: 16, 24, 32, 64) and logo192.png (192x192 PNG). Use any icon generator or create a minimal SVG-to-ICO conversion. |
| 10 | B1: Error notification styling | `src/index.css` | S | Add `.notification--error { border-left-color: #ef4444; }` and `.notification--error svg { stroke: #ef4444; }` after the `.notification` rules near line 434. |
| 11 | S2: Humor engine entropy disclosure | `src/components/PassphraseDisplay.jsx` or `src/App.jsx` | M | Either: (a) add a subtle indicator when humor mode was used ("funny mode — lower entropy"), or (b) document the entropy tradeoff in the UI/README, or (c) make humor mode a toggle so users can opt for pure diceware. Decide approach with user. |

## Phase 4: Configuration & Build

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 12 | C1: Dockerfile uses npm install | `Dockerfile:13` | S | Change `RUN npm install` to `RUN npm ci` for reproducible builds. |
| 13 | C2: Add ESLint config | Project root → new `eslint.config.js` | M | Create flat ESLint config for React/Vite. Install `eslint` and `eslint-plugin-react` as devDependencies. Add `npm run lint` script to package.json. |
| 14 | Q3: Archive project-plan.md | `project-plan.md` | S | Delete the file or move to `docs/archive/`. It's a historical v1.0 planning document with outdated pseudocode. |

## Phase 5: Nice-to-Haves & Hardening

| # | Finding | Files | Effort | Description |
|---|---------|-------|--------|-------------|
| 15 | S1: Self-host Google Fonts | `index.html:15-18`, `src/index.css` | M | Download Inter and JetBrains Mono woff2 files, place in `public/fonts/`, add `@font-face` declarations to `index.css`, remove Google Fonts `<link>` from index.html, update CSP in nginx.conf to remove fonts.googleapis.com. |
| 16 | A1: Add skip-to-content link | `index.html` or `src/App.jsx` | S | Add a visually-hidden skip link before the h1 that targets `#passphrase-display` or similar. |
| 17 | A2: Toggle switch colorblind accessibility | `src/index.css:309-351` | S | Add a visual indicator beyond color for the toggle on-state — either a subtle size change, a checkmark pseudo-element, or different border style. |
| 18 | D1: Add GitHub Actions CI | `.github/workflows/ci.yml` | M | Create workflow: install deps, run tests, run build on push/PR to main. |
| 19 | D2: Add pre-commit hooks | `package.json` | S | Install husky + lint-staged, configure to run ESLint on staged .js/.jsx files. Depends on item 13. |
| 20 | D3: Add Dependabot config | `.github/dependabot.yml` | S | Configure weekly npm dependency checks. |
| 21 | D4: Version-tagged Docker builds | `CLAUDE.md`, `README.md`, deploy script | S | Update build command to: `docker build -t password-generator:$(node -p "require('./package.json').version") -t password-generator:latest .` |
| 22 | D5: Add .env.example | Project root | S | Create file with single comment: `# No environment variables required for this app` |
| 23 | O2-O4: Performance micro-optimizations | Various | L | Pre-bucket words by length, pre-filter humor templates by feasible length range, promote passphrase-panel to compositor layer. Only pursue if generation latency becomes noticeable. |

## Dependencies & Ordering Notes

- **Item 3 (extract secureRandomInt)** should be done before item 4 (humorEngine tests) to avoid testing the duplicate function.
- **Item 13 (ESLint config)** must be done before item 19 (pre-commit hooks).
- Items 1, 2, 3 are independent and can be done in parallel.
- Items 4, 5, 6 are independent and can be done in parallel.
- Item 9 (favicon) can be done anytime — no dependencies.

## Quick Wins (S effort + High/Medium severity)

1. **H1** (nginx headers): ~10 minutes — copy-paste 4 lines into the location block
2. **M1** (requirements length): ~15 minutes — 3-line change in App.jsx + 1 test
3. **O1** (extract secureRandomInt): ~15 minutes — new file, two import changes
4. **M4** (README): ~15 minutes — text updates
5. **Q1** (structure YAML): ~15 minutes — text updates
